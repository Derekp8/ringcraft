# M9 Handoff Evidence

## Package

- Archive: `asw91-project-ringcraft-m9-private-handoff-1.2.0.zip`
- Archive identity (rebuilt 2026-08-19T12:42Z from the LF-normalized working tree — HEAD `b54794c` plus the uncommitted M13 follow-up work: the title-shot chain fixture's `grantExtraTitleShot` manual-booking leg, the full-card month-end banner, the seeded world-tag defense replay pin, the grant-line panel, and the extra-shot schedule line — built through an LF staging copy so every pinned file is its LF representation and the manifest pins match the archive bytes directly): **42,499,735 bytes**, **175 files** (all 21 reviewed screenshots embedded), **SHA-256 `e6900876fd8c4600`**.
- This rebuild supersedes the `4d6f57da…3ea9a` archive (built 2026-08-17 from the committed tree at HEAD `3a99d12` — exported with `git -c core.autocrlf=false archive`, so every pinned file is its LF-blob representation and the manifest pins match the archive bytes directly — **41,025,879 bytes**, **171 files**, SHA-256 `4d6f57da79ae624f`), which superseded the `2dadebbb…227b` archive (which superseded the `71a65407…fe087` archive (which superseded `17fa57ed…3003`, which superseded `f4c678a1…87cc6`, which superseded `31da8609…aadee4`, which superseded `308abea0…5133`, which superseded `311d2b16…ac5d`, which superseded `e026602c…1ef7`, which superseded `1a1579c4…c008`, which superseded `a1c71da1…0096`, which superseded `fd25e4dd…432d`, which superseded `fa8d0902…19a2`, which superseded `feee235a…00a80`, which superseded `4b098eb2…0398`, which superseded `3ede70b9…da75`)) and reflects the fully committed tree at `3a99d12`, which adds the M13 feud-and-title-booking layer (design spec `abf881f`; feud/booking core `b4e310f` — feud heat/decay with the M13-ADJ-03 title-shot term, M13-ADJ-02 month-end booking cards, `startFeud`; the contract-negotiation amendment `1a69fe0` + its design spec; the feud and booking-card dashboard panel `aa8a145` with pin reconciliation `6f78539`; and the tag-team feud rival tests `3a99d12` — heat movement, decay/revival, booking-card items and booking preference for a player team — moving the suite baseline to **388 tests / 23 files**) and, from the previous rebuild, the pixel-diff stability gate (`scripts/check-visual-qa-stability.mjs` — runs the visual QA gate twice and asserts every capture reproduces byte-identically or within the documented ±2/255 sub-pixel AA-jitter tolerance, with a zlib-based PNG decoder and per-capture comparison against both the run-1 snapshot and the committed LF-blob pins; pin mismatches are informational on non-Linux renderers since the pins are canonical-Linux renders) and the paired Windows Edge-fallback verification record for the preceding identity (`output/m9/m9-verification-windows.json`); on top of the earlier rebuilds' contents: the save-manager remote-sync follow-up (`0017e37`: the last-synced baseline line read from the sync meta, per-campaign merge diff hints in the import-bundle preview, the force-push conflict-resolution leg in the QA gate, and the deterministic QA identity freeze that makes the bundle fingerprint byte-reproducible across runs) and its manifest-pin reconciliation (`4188b77`), the canonical paired verification records for the preceding identity (`581325a`), and the reviewed save-manager/remote-sync screenshot captures (`562ddee`) — the suite baseline moves to **304 tests** — on top of the earlier rebuilds' contents: its evidence docs, the tuned ladder, and the strengthened M11 playtest gate: the playtest balance report (`a12947d`) now carries a 95% Wilson confidence interval on every win share (`winShareCI`, computed from the per-seed outcomes as a pure deterministic function — `novice 0.031 [0.006, 0.157]`, `standard 0.097 [0.033, 0.249]`, `veteran 0.207 [0.098, 0.384]`, `ruthless 0.563 [0.393, 0.718]`), re-pinned to `reportHash c14n-fnv1a64-v1:0cf1a58e2b994c0a`, with the statistical claim that ruthless's entire interval lies above every lower rung's; the report additionally carries a difficulty-vs-difficulty head-to-head sweep (`analytics.headToHead.byPair`, every unordered pair of ladder rungs played on the equal end-28 roster with the lower rung driving the player side): the upper three rungs are a transitive total order — the higher rung wins 100% of decisive matches in `standard<veteran`, `standard<ruthless`, and `veteran<ruthless` — while novice is the documented out-of-ladder anomaly, its 35% mistake noise beating standard (0.667) and ruthless (0.563) directly on equal rosters with veteran still edging it (0.548); the tuned ladder: the novice-end sweep (`3208e66`) exported and pinned `NOVICE_MISTAKE_RATE = 0.35` (empirically swept across the same six seed offsets as the depth-2 blend — 0.25 collapses at offset 1000, 0.40 thins to 0.006, 0.45+ over-hardens) and hardened the weak-end gate (per-window `novice < standard − 0.02`, aggregate `− 0.10`), and the veteran-end sweep (`6fa0ba2`) confirmed the `standard<veteran` rung across weights 0.05–0.30 (min margin 0.105–0.232), hardening its gate to per-window `− 0.10` / aggregate `− 0.20` at the unchanged 0.15 weight — the default policy is byte-identical, so every replay hash is unchanged. On top of the earlier rebuilds' contents: the strengthened clean-room gate asserting the M10 corpus's per-decision hash column and `finalStateHash` coverage explicitly (`verify-m10-corpus.ts` reports `hashColumnChecked`/`finalStateHashesReplayed`, the manifest pins both counts as `deterministic_evidence`, and `verify-m9-handoff.ts` parses the corpus verifier's JSON from the `fixtures:verify` output and asserts the counts numerically rather than by substring); the whole-corpus decision-locator diff `scripts/diff-m10-corpus.ts` with mutation-fault-injection tests; the ruthless-campaign replay pin promoted to the fixture system with generator/verifier and a shared derivation module, and the campaign-level replay contract (committed match `5e29acd05f33fb9c`, month-end final `64936edc8e1bd790`, stored replay `7944d0ddd897bcfe`); the M12 contracts-and-finance UI surface with the popularity/chemistry amendments M12-ADJ-04/05; the M11 match-variety milestone (cage/ladder engine, fixtures, balance report, trend report); the save-manager determinism fixture; the consecutive-run pixel-stability guard and deterministic QA gate with frozen page clock; the restore/load rollback-diff preview; the M10 mid-match difficulty disclosure plus tour ladder line and pinned replay identities; the golden ruthless/tag replay pin tests and the replay-versioned verifier with committed fixtures; the hash-line truncation fixes; the save-manager autosave-retention work; and the regenerated reviewed screenshots. The prior rebuild folded in the M10-ADJ-05 ladder-gate trim (`800ae68`, 32 seeds per window with all six offsets retained and every margin re-measured at the trimmed size — per-window novice 0.034 / standard<veteran 0.151 / veteran<ruthless 0.063, aggregate 0.112 / 0.299 / 0.187), its docs-and-pins record (`41e4f27`), the refreshed canonical-Linux review screenshots (`0d3d658`), and the `markDiscardable` gate speedup (`097bb69`): the separation test's headless loop now advances a caller-owned cursor in place (no defensive JSON clone per player turn, no per-event full-state hash), with decisions, RNG stream, input log, and `hashMatchState` proven byte-identical to the live path — dropping the clean-room `npm run check` from 90.3 s to 43.2 s. This rebuild additionally folds in the remote-sync merge-resolution E2E test (`292db5d`, pushing a bundle over real HTTP, simulating a concurrent-writer conflict, and resolving it by campaign-aware import + force push — the suite baseline moves to **302 tests**, 20 files), the manifest-pins CI gate (`14219de`, `scripts/check-manifest-pins.mjs` hashes every `critical_file_sha256` entry against its LF git blob and fails on any mixed-representation pin, wired as a `manifest-pins` workflow; it immediately caught and this rebuild includes the refresh of the evidence-doc pin `1397a4c`), and the archive now carries the pin-integrity script itself (149 files).
- **Manifest reconciliation (this tree):** all 68 `critical_file_sha256` pins are the LF-blob representation of the tree the archive was built from — the LF-normalized working tree carrying the refreshed evidence, enforced going forward by the `manifest-pins` CI workflow (`scripts/check-manifest-pins.mjs`) — and every pin matches the archive's embedded bytes (68/68, including the 21 reviewed-screenshot byte-pins), on Windows extraction and in the Linux clean room alike. `verification_counts` and `included_evidence` were refreshed to the built tree (21 test files, 342 tests; M10/M11/M12/saves/replays artifacts listed), and `deterministic_evidence` includes `fixture_ruthless_campaign_final_hash = c14n-fnv1a64-v1:64936edc8e1bd790`, the M10 corpus coverage pins `m10_corpus_hash_column_checked = 1050` (every decision carries its per-decision hash column) and `m10_corpus_final_state_hashes_replayed = 12` (every run replays its `finalStateHash`) — the 1,050-decision corpus with the seeded ruthless run committed on top of the `7a8c4cea…182d` build, whose clean-room gate verified the preceding 1,045-decision corpus (see the gate table and corpus pin below), and the four `m11_playtest_win_share_*` pins (`0.03125` / `0.0967741935483871` / `0.20689655172413793` / `0.5625`), plus the M11 playtest `reportHash` (`m11_playtest_report_hash = c14n-fnv1a64-v1:0cf1a58e2b994c0a`) and the save-manager determinism `fixtureHash` (`save_determinism_fixture_hash = c14n-fnv1a64-v1:bf53d640d63921b9`).
- The checksum is intentionally not embedded in the archive manifest (a container cannot embed its own final SHA-256 without changing that SHA-256); it is recorded here and in the machine-readable `output/m9/m9-build.json` / `output/m9/m9-verification.json`.
- Archive contents: allowlisted source, tests, scripts, fixtures (M5, M10, M11, saves, replays), documentation, references, package files, and reviewed screenshots only.
- Exclusions: dependencies, build output, local `.freebuff` state, browser runtimes/caches, temporary files, and unreviewed generated output.

## Verification Record (2026-08-17 clean-room run)

Built by `scripts/build-m9-handoff.mjs` and verified by `scripts/verify-m9-handoff.ts` inside a fresh clean-room extraction. Every gate exited 0.

### Canonical Linux clean-room run ledger

Every recorded canonical Linux run for the six identities that precede this archive's line, from the retained logs (the per-archive gate tables in the committed evidence-doc revisions listed, plus the committed Windows-fallback machine record where no Linux table was retained). All runs: Ubuntu 24.04.4 LTS on WSL2, Node v24.19.0, `browserTarget: linux-portable-chromium`, `canonicalLinuxBrowserVerified: true`.

| Archive SHA-256 (size) | Built (UTC) | Source commit | `npm ci` | `npm run check` | `fixtures:verify` | `visual-qa.mjs` | Recorded in |
|---|---|---|---|---|---|---|---|
| `e6900876fd8c4600b5e038ff4cdcb608ab84515442cefcccb68fbc73fcfced3b` (42,499,735 B, 175 files) | 2026-08-19 12:42Z | working tree (HEAD `b54794c` + uncommitted M13 follow-up) | exit 0 · 2 s | exit 0 · 180 s (25 files, 432 tests) | exit 0 · 356 s (8/8, chain `3e154f113603bba6`) | exit 0 · 429 s | this doc (2026-08-19) |
| `4d6f57da79ae624f441465797918aa56c85dac050b3849bda14b4e61dfd3ea9a` (41,025,879 B, 171 files) | 2026-08-17 19:18Z | commit `3a99d12` | exit 0 · 1.9 s | exit 0 · 179.5 s (23 files, 388 tests) | exit 0 · 341 s (8/8, pin `64e1f4af`) | exit 0 · 221.2 s | doc `79498b2` |
| `733e652e7b04d618c3d75de9fba587c4b33c94df4afe836f660361b4196b19ee` (29,858,609 B) | 2026-08-14 12:35Z | pre-commit working tree | exit 0 · 3.7 s | exit 0 · 38.6 s | exit 0 · 5.7 s (M5 + M10 corpus, pin `80421f1d`, 11 final-state hashes) | exit 0 · 133.6 s | doc `c3a4ab3d` |
| `fa4d6d46a138840bec67cf238e50bf3f1fac558b511ceeb0743640970ce589f4` | 2026-08-14 13:16Z | pre-commit working tree | — *(Linux table not retained)* | — | — | — | prose only (doc `9e21a44`); Windows fallback machine record `19fddfc8` |
| `28c1560163c117f849901d6c34d5dad28bb7c025a5ba548978918db3fbe1dd6d` (30,097,230 B) | 2026-08-14 14:55Z | pre-commit working tree | exit 0 · 1.9 s | exit 0 · 42.1 s | exit 0 · 56.0 s (4/4, pin `80421f1d`) | exit 0 · 132.2 s | doc `9e21a44` |
| `5b913222193d9697fe406b4b7997241db28a26fa460ac257d769f13e8b8c14a4` (30,421,519 B, 126 files) | 2026-08-14 18:55Z | commit `8b8a7ca` | exit 0 · 1.7 s | exit 0 · 83.0 s | exit 0 · 59.9 s (5/5, pin `80421f1d`) | exit 0 · 130.9 s | doc `7f691ab` |
| `532b0334f55e113a0a38079c1569a3cc22bf39a5e642b5830f336767abc9dfec` (30,572,180 B, 133 files) | 2026-08-14 20:50Z | commit `6a68628` | exit 0 · 1.8 s | exit 0 · 93.2 s | exit 0 · 60.7 s (6/6, pin `80421f1d`) | exit 0 · 194.4 s | doc `6a127c2` |
| `be9cef4f1da1c942208193ad71f08c47cd09358a0c4f33819686bc5132dd653e` (30,573,146 B, 133 files) | 2026-08-14 21:14Z | commit `6a127c2` | exit 0 · 1.8 s | exit 0 · 93.2 s | exit 0 · 60.7 s (6/6, pin `80421f1d`) | exit 0 · 194.4 s | doc `624f99b` |

Provenance notes:

- **fa4d6d46** is the one identity with no retained Linux gate table — its canonical Linux run is recorded only in prose ("recorded earlier on 2026-08-14 on the same host" in doc `9e21a44`). Its retained machine record is the **Windows-fallback** run committed at `19fddfc8` (`browserTarget: windows-edge-fallback`, `canonicalLinuxBrowserVerified: false`): `npm ci` 4.4 s, `npm run check` 53.3 s (10 files, 191 tests), `fixtures:verify` 6.2 s (2 verifiers), `visual-qa.mjs` 162.9 s — all exit 0.
- **532b0334** additionally carries a 2026-08-16 re-run (npm ci 2.3 s, check 93.2 s, fixtures 68.0 s, visual-qa 203.8 s — all exit 0, ruthless `43945f1cc482e0cd` + tag `1b26c32a342f08c8` reproduced) recorded in the fresh machine record `output/m9/m9-verification.json`.
- **733e652e's** gate table sits under a "Duration" header (the platform-neutral table in doc `c3a4ab3d`, which also records a Windows-fallback run for the same identity); the durations shown follow the canonical-Linux paragraph and the portable-Chromium visual-qa row, so they are the Linux run's.

**Canonical Linux run (this archive `e6900876…4600`):** Ubuntu 24.04.4 LTS on WSL2, Node v24.19.0, `browserTarget: linux-portable-chromium`, `canonicalLinuxBrowserVerified: true` — the portable Chromium (`@sparticuz/chromium`, Linux ELF) executed the full browser profile list inside a fresh clean-room extraction with no system-browser fallback, `npm run check` 180 s (25 files, 432 tests — including the manual-booking-leg chain tests in `tests/replay-verifier.test.ts`), `fixtures:verify` 356 s (8/8 verifiers, the title-shot chain fixture re-derived at `3e154f113603bba6` with the `grantLine` and `extraGrantLine` fields asserted), `visual-qa.mjs` 429 s (seeded ruthless `c14n-fnv1a64-v1:43945f1cc482e0cd` + tag `c14n-fnv1a64-v1:1b26c32a342f08c8` replay pins reproduced). Machine-readable record: `output/m9/m9-verification.json`.

**Windows fallback run (this archive `e6900876…4600`):** Windows 11 host, Node v26.5.0, `browserTarget: windows-edge-fallback` — the system Edge (Chromium) executed the full browser profile list (portable chromium unavailable on Windows: Linux ELF); every gate exited 0 — `npm ci` 4.6 s, `npm run check` 180.5 s (25 files, 432 tests), `npm run fixtures:verify` 333.6 s (8/8, chain `3e154f113603bba6`), `node scripts/visual-qa.mjs` 503.3 s (seeded ruthless `c14n-fnv1a64-v1:43945f1cc482e0cd` + tag `c14n-fnv1a64-v1:1b26c32a342f08c8` replay pins reproduced; 22 captures, pixel-stability baseline established on first run). Machine-readable record: `output/m9/m9-verification-windows.json`. This pairs the archive's canonical Linux record: the same `e6900876…4600` bytes verified on both the portable-Chromium Linux host and the Edge-fallback Windows host. The canonical Linux runs for the preceding archives (`4d6f57da…3ea9a`, `b7d6e5c7…38e5a`, `2dadebbb…227b`, `ed97cd43…f5909`, `71a65407…fe087`, `17fa57ed…3003`, `f4c678a1…87cc6`, `31da8609…aadee4`, `308abea0…5133`, `311d2b16…ac5d`, `e026602c…1ef7`, `1a1579c4…c008`, `a1c71da1…0096`, `fd25e4dd…432d`, `fa8d0902…19a2`, `feee235a…00a80`, `4b098eb2…0398`, `3ede70b9…da75`, `be9cef4f…653e`, `733e652e…`, `fa4d6d46…`, `28c15601…`, `5b913222…c14a4`, `532b0334…dfec`) were recorded earlier on 2026-08-14/15/16/17/19 on the same host.

**Windows fallback run (this archive `2dadebbb…227b`):** Windows 11 host, Node v26.5.0, `browserTarget: windows-edge-fallback` — the system Edge (Chromium) executed the full browser profile list (portable chromium unavailable on Windows: Linux ELF); every gate exited 0, reproducing the seeded ruthless/tag replay pins (`c14n-fnv1a64-v1:43945f1cc482e0cd` / `c14n-fnv1a64-v1:1b26c32a342f08c8`) and 21 captures recorded. Machine-readable record: `output/m9/m9-verification-windows.json`. Earlier Windows fallback runs were recorded for the `17fa57ed…3003`, `311d2b16…ac5d`, and preceding identities.

**Strengthened-contract coverage (this archive):** the clean-room verifier asserted all 68 `critical_file_sha256` pins against the archive's embedded bytes — including all 21 reviewed-screenshot byte-pins (each capture's SHA-256, listed below) — and the manifest-pins CI workflow hashes the same 21 PNGs against their committed LF blobs, so a change to any committed screenshot fails both gates, not just the run-to-run pixel-stability baseline. The full gate also ran inside the clean room against the built tree's tests, so the campaign-level replay pin, its month-end extension, and the corpus-locator mutation tests are covered by the canonical gate: `npm run check` executed the new assertions `✓ pins the final campaign hash after the committed ruthless match (campaign-level replay contract)`, `✓ … after advancing a full month with the ruthless match committed (month-end replay contract)`, and the two mutation-fault-injection tests (`✓ names the exact seed, tick, and kind when a single decision hash is flipped in a scratch copy` and `✓ reports every diverged decision across all runs, not just the first`) plus the golden ruthless/tag replay-hash pins and the playtest interval and head-to-head tests (`✓ carries a valid 95% Wilson confidence interval on every win share`, `✓ separates the ruthless rung with 95% confidence from the entire lower ladder`, `✓ pins the head-to-head strength ladder: the upper rungs are transitive on equal rosters, novice is out-of-ladder`, `✓ documents the novice vs ruthless evidence line: the noise anomaly, not top-rung dominance`, and `✓ re-derives the pinned head-to-head pair live from the seeded equal-rosters sweep`), and the save-manager follow-up tests (the last-synced baseline `syncedAt` stamping and the per-campaign merge-diff hint classification) (23 test files, 388 tests), `npm run fixtures:verify` re-derived all eight verifier suites (M5, M10 corpus pin `80421f1d`, **M10 ruthless-campaign fixture**, M11 fixtures, M11 playtest report (reproducing `reportHash c14n-fnv1a64-v1:0cf1a58e2b994c0a` including `winShareCI` and the head-to-head `byPair` sweep), M11 playtest trend, save-manager determinism, replay documents — 8/8 `status: "verified"`, `driftCount: 0`), and `visual-qa.mjs` reproduced the seeded ruthless replay pin `c14n-fnv1a64-v1:43945f1cc482e0cd` and the seeded tag replay pin `c14n-fnv1a64-v1:1b26c32a342f08c8`. The pixel-stability guard established its baseline on the clean room's first run and passed.

| Gate | Result | Duration (Linux, this archive) | Duration (Windows fallback, this archive) |
|---|---|---|---|
| `npm ci` (lockfile preserved) | exit 0 | 1.9 s | 4.6 s |
| `npm run check` (full test suite + build) | exit 0 | 180.1 s | 180.5 s |
| `npm run fixtures:verify` (M5 + M10 corpus + M10 ruthless-campaign + M11 fixtures + playtest report + playtest trend + save determinism + replay documents) | exit 0 — 8/8 `status: "verified"`, corpus pin `64e1f4af`, `driftCount: 0` | 355.9 s | 333.6 s |
| `node scripts/visual-qa.mjs` (full browser profile list; portable Chromium on Linux, Edge fallback on Windows) | exit 0 | 429.5 s | 503.3 s |

The paired Windows fallback run for this archive's identity `e6900876…4600` is recorded in `output/m9/m9-verification-windows.json` (Windows 11, Edge fallback, all gates exit 0, 22 captures), and the paired Windows run for the preceding `2dadebbb…227b` identity remains recorded in the same file's earlier revision lineage; the paired Windows runs for the `b7d6e5c7…38e5a` and `4d6f57da…3ea9a` identities are pending.

The verifier additionally asserted:

- Archive SHA-256 `4d6f57da79ae624f`; all ZIP entries store-only; allowlist, required files/roots, and all 21 reviewed screenshots present; no excluded or duplicate paths; the M13 feud/booking core (`src/core/campaign.ts`, `campaign-rules.ts`, `types.ts`), the feud dashboard panel (`src/ui/panels.tsx`, `App.tsx`, `styles.css`), the tag-team feud tests (`tests/m13-feud-booking.test.ts`), and the pixel-diff stability gate script `scripts/check-visual-qa-stability.mjs` all embedded and allowlisted.
- Every `critical_file_sha256` pin in the archive's `HANDOFF-MANIFEST.json` matches the extracted file (68/68 — including the four `m11_playtest_win_share_*` pins, the refreshed `fixtures/saves` pins, and all 21 reviewed-screenshot byte-pins), and `package-lock.json` is byte-identical before and after `npm ci`.
- The portable Chromium (`@sparticuz/chromium`, Linux ELF) executed the full browser profile list inside the clean room; no system browser fallback was involved.
- The manifest's `deterministic_evidence` keys — including `fixture_ruthless_campaign_final_hash`, `m10_corpus_hash_column_checked`, `m10_corpus_final_state_hashes_replayed`, and the four `m11_playtest_win_share_*` pins — appear in the clean room's `fixtures:verify` output alongside the M5 evidence hashes. The corpus coverage counts are asserted by parsing the corpus verifier's JSON from the `fixtures:verify` output (not just substring matching), so a wrong hash-column or final-state coverage count fails the gate. The M11 playtest verifier likewise exposes the re-derived per-difficulty win shares as a dedicated `ladderWinShares` field that the gate parses: each pinned `m11_playtest_win_share_*` value must match numerically, the strict `novice < standard < veteran < ruthless` ordering must hold on the re-derived numbers — so the clean-room manifest check covers the ladder ordering independently of the unit separation gate (`0.03125 < 0.0967741935483871 < 0.20689655172413793 < 0.5625` at this identity) — and the re-derived `reportHash` must equal the pinned `m11_playtest_report_hash` exactly. The save-determinism verifier's JSON is likewise parsed: its re-derived `fixtureHash` must equal the pinned `save_determinism_fixture_hash` exactly, so a change to the merge rule, the ring retention cap, the remote-sync revision/fingerprint logic, campaign hashing, or payload serialization fails the clean-room gate with the pinned-vs-actual hash.
- Machine-readable records: `output/m9/m9-verification.json` (canonical Linux run for this `4d6f57da…3ea9a` identity — all four gates exit 0, 388 tests / 23 files, ruthless `43945f1cc482e0cd` + tag `1b26c32a342f08c8` replay pins reproduced, the M13 feud-panel QA leg passed with the post-commit hash `724838a26aff9fb1` re-pinned); `output/m9/m9-verification-windows.json` holds the Windows fallback record for the `2dadebbb…227b` identity, with the paired runs for the `b7d6e5c7…38e5a` and `4d6f57da…3ea9a` identities pending.

One verifier change landed with the preceding rebuild: the clean-room command-output retention was widened from 4,000 to 24,000 characters (`8b8a7ca`). The eight-verifier fixture chain's combined stdout exceeds the old retention, which truncated the M5 deterministic-evidence hashes out of the retained tail and failed the manifest evidence check despite every verifier reporting `verified`.

## Known Deterministic Evidence

- Completed campaign: `c14n-fnv1a64-v1:1a11f20c552d50ba`
- Completed replay: `c14n-fnv1a64-v1:b5cc3ccccd2e25ee`
- Recovered continuation: `c14n-fnv1a64-v1:bd2c470ca5bf286a`
- Twelve-month campaigns: solo `c14n-fnv1a64-v1:af6ff3ca602fc41a`, tag `c14n-fnv1a64-v1:a4635a8085b77a8c`
- M10 decision corpus: `m10-ai-decision-log-v1`, pinned by `fnv1a32` → `64e1f4af` (1,050 decisions), with a per-decision hash column and per-run terminal `finalStateHash` (both replayed identically by the `fixtures:verify` chain; the `4d6f57da…3ea9a` clean-room gate verified this 1,050-decision corpus), including the seeded (1991) `ruthless-singles-1991` run — replayed under the 2-ply policy by the difficulty-aware selector, with golden final state `c14n-fnv1a64-v1:7c2785c8f2aee021` — so the ruthless replay contract is pinned by the fixtures gate as well as the visual gate and the unit-suite golden pins
- M10 ruthless-campaign fixture: `fixtures/m10/ruthless-campaign-v1.json`, re-derived by its verifier — committed match `c14n-fnv1a64-v1:5e29acd05f33fb9c`, **month-end final `c14n-fnv1a64-v1:64936edc8e1bd790`**, stored replay `c14n-fnv1a64-v1:7944d0ddd897bcfe` (also asserted by the M9 clean-room verifier's evidence-key check)
- M11 playtest balance report: `fixtures/m11/playtest-balance-report-v1.json`, `reportHash c14n-fnv1a64-v1:0cf1a58e2b994c0a` (win-share ladder with 95% Wilson confidence intervals — ruthless's interval lies wholly above every lower rung's — plus match-length and finish-method analytics and the difficulty-vs-difficulty head-to-head `byPair` sweep: upper rungs transitive, novice the documented out-of-ladder anomaly, all re-derived by the verifier)
- Save-manager determinism fixture: `fixtures/saves/save-determinism-v1.json`, `fixtureHash c14n-fnv1a64-v1:bf53d640d63921b9` (bundle-merge rule, autosave ring incl. the retention-cap prune leg, and the remote-sync arc — including each step's `SyncResult.message` and the persisted `SyncMeta` baseline of the last sync's fingerprint and remote revision — re-run from the pinned inputs)
- Replay documents: `fixtures/replays/ruthless-seed-1991-v2.json` and `fixtures/replays/tag-seed-1991-v2.json`, replayed through the current engine by `verify-replay.ts` with zero schema/data/hash drift
- Respond-title-shot event chain: `fixtures/replays/title-shot-chain-v1.json` (schema `m13-title-shot-chain-v1`, `fixtureHash c14n-fnv1a64-v1:3e154f113603bba6`), the campaign-level corpus companion — the seeded tag-champion scenario's grant → decline / grant → accept chains plus the **manual-booking leg** pinned as fixture evidence (offer `title-shot-4c1632ac`, the consolidated roll line `6 -3 same side (tag) +3 feud heat 60 vs champion = 6`, the grant-event line as a first-class `grantLine` evidence field — the shared `titleShotGrantLine` helper's output, re-derived by the verifier and asserted inside the recorded grant detail so the log and decisions panel provably cannot drift — plus the grant/decline/accept event details, the four campaign-hash chain links, the respond events' pre/post hashes, the scheduled mandatory defense, and the symmetric manual leg: the champion plays the defense (pin, `finalMatchHash c14n-fnv1a64-v1:11b115985c980250`, obligation 1/1 complete) then grants an extra shot via `grantExtraTitleShot`, with the `extraGrantLine` (the shared `titleShotExtraGrantLine` helper's output) asserted inside the schedule event detail, the extra-shot row, and the extra grant event's pre/post hashes), re-derived by the replay verifier on every `fixtures:verify` run and asserted against the manifest's `m13_title_shot_chain_fixture_hash` pin by the clean-room gate
- Feud-heat event chain: `fixtures/replays/feud-heat-chain-v1.json` (schema `m13-feud-heat-chain-v1`, `fixtureHash c14n-fnv1a64-v1:9d6510b827550cb5`), mirroring the title-shot chain design — the feud ledger's full lifecycle pinned as fixture evidence (start-feud → committed feud match → cold month's monthly decay; all titles vacant so the chain isolates feud mechanics): `startFeud` opens the t1 vs t2 rivalry at heat 60 (the start-feud transaction's pre/post hashes are chain links), the headless feud match resolves to a deterministic time-limit draw (heat 60 → 64, `reason "draw"`, `finalMatchHash c14n-fnv1a64-v1:6ab6b17466e0a491`, commit detail line `Feud championship tag grudge (t1 vs t2): heat 60 → 64 (+4); 1 feud match(es).`), the matched month provably never cools (`matchedMonthNoDecay`: heat 64, one movement row), and the cold February decays 64 → 59 (`reason "monthly-decay"`, log line `Feud championship tag grudge cooled 64 → 59 (no match in 1991-02).`) — with the feud identity `feud-302eaae0`, both movement rows, the final feud state (59, active, 1 match, last 1991-01-01), and the six campaign hashes that form the chain links (initial → feuded → scheduled → committed → Feb 1 → Mar 1), re-derived by the replay verifier on every `fixtures:verify` run and asserted against the manifest's `m13_feud_heat_chain_fixture_hash` pin by the clean-room gate
- Seeded ruthless exhibition replay (seed 1991, `ruthless`): `c14n-fnv1a64-v1:43945f1cc482e0cd`, replayed externally from the exported input log by the visual QA gate
- Seeded tag-team exhibition replay (seed 1991, `standard`): `c14n-fnv1a64-v1:1b26c32a342f08c8`, replayed externally from the exported input log by the visual QA gate
- Seeded world-tag defense match (tag-feud-career profile, 1991-01-02): `c14n-fnv1a64-v1:0707c852c4025914` — a time-limit draw (career-team-2 vs champion career-team-1, champion retains), pinned as `WORLD_TAG_DEFENSE_REPLAY_HASH` in the visual QA gate: the latest-official-result card must surface the exact hash and the committed match's stored replay (244 inputs, read from the autosave snapshot) must re-derive to the same identity externally

## Reviewed Screenshot Byte-Pins

All 21 tracked review screenshots are byte-pinned: each is embedded in the archive and its SHA-256 is a `critical_file_sha256` entry, so the clean-room verifier hashes the archive's extracted bytes against the pin and the `manifest-pins` CI workflow hashes the committed LF blob — the byte-identical guarantee is enforced by the gates, not just by the run-to-run pixel-stability baseline.

| Screenshot | SHA-256 |
|---|---|
| `output/qa/ringcraft-accessibility.png` | `1cc94f971de107bbd52b581ced414b39e78c02dd0d0ee63b07e561d018aee9a8` |
| `output/qa/ringcraft-career-desktop.png` | `7ee822cae4b4e46a81c49c672c42c7ae1204c7c9cbc9145a7d47345b76c7a7cf` |
| `output/qa/ringcraft-career-match.png` | `e336a4a94dbde07fd2f45b4b6d056f789fca0ff368846cdda003bbab57610fc5` |
| `output/qa/ringcraft-career-narrow.png` | `2621e522e73f96fd8baccdc043a449328b31800f54ef36446e30ac1057d15448` |
| `output/qa/ringcraft-career-post-match.png` | `598bbf16bc9b3868f99c9cc21fa80681f44a208825cb2f8bb5a8c42bfa7d411a` |
| `output/qa/ringcraft-career-recovery.png` | `7df32f9e084a580a48fd45240586a974c0c58b385e752f676222bf5a12f05952` |
| `output/qa/ringcraft-career-setup.png` | `7a837cb306dc556018eeb645fc6d2d770da416b7a2abfa7c6b209fb2f978ec50` |
| `output/qa/ringcraft-created-exhibition.png` | `2febf21d24231c7ae9d7bccb0c0c4316d659db3a409059971ab6aac78c271b0b` |
| `output/qa/ringcraft-creator-desktop.png` | `dedd130de760c146ccdfdaabb3dbb29e9f6ca49e61edf9dad4513a0fc24fe977` |
| `output/qa/ringcraft-creator-narrow.png` | `9671513bfc1d7556686091de5db77594bf18b889a86198f5627af6aa1b2a82b1` |
| `output/qa/ringcraft-creator-validation.png` | `e8f7dbeee2a1c1036aa1a11f98e27be74146a55466067344db84bbf0fda9a31f` |
| `output/qa/ringcraft-help-toggle.png` | `93ae3c20635188f5bff05009c5109a19c2810930795ec2822163be94391821de` |
| `output/qa/ringcraft-m10-difficulty-career.png` | `efd98aab5c322457f6eaefa6e2a8c77512504bea1ab2848c66f01db150c10ff2` |
| `output/qa/ringcraft-m10-difficulty-exhibition.png` | `879c0c8d5ba4716cb461c4648c28b30ac4cddf25658b7944efc67a585861ad27` |
| `output/qa/ringcraft-progression.png` | `90cdfd118d6a75796264858c159eb96a8821483ff12172e8e15397aed659c73d` |
| `output/qa/ringcraft-rules-lab.png` | `b58256d80bb28c7aa54e4ab943b2cb1520d8e5aeac2f24a6d1197d1b035dd761` |
| `output/qa/ringcraft-save-overwrite.png` | `689fbc67fcd8829fc3efc154dee04202c78dc2f3e093c274c6a4910bdc534012` |
| `output/qa/ringcraft-singles-desktop.png` | `59229b752b0c97a622d9650b1737b1c0d8bbe87d7491f683871aa066f5247beb` |
| `output/qa/ringcraft-tag-desktop.png` | `0e13f29e8c082d096dbac6f95a22844d92a77b1ccd39574563855c99f824e080` |
| `output/qa/ringcraft-tag-narrow.png` | `cc0dc4268f957c016437b8e4b962066c1d098921300a33493562295e71261049` |
| `output/qa/ringcraft-tour.png` | `67aceeb86b8bc62625d0fdaa8a631e012cffe79df6b69b022a7f439152c5eabb` |

The 8 career/recovery/accessibility/tour captures were already in the archive's reviewed list; the other 13 (created-exhibition, creator-*, help-toggle, m10-difficulty-*, progression, rules-lab, save-overwrite, singles/tag-*) were added so every byte-pinned capture is embedded and gate-asserted.

## External Gates

- Human screen-reader and assistive-technology review remains pending.
- Keyboard-only, zoom/reflow, forced-colors, and reduced-motion human review remains pending.
- Independent source transcription and adjudication remain pending.
- Playtest-led balance and pacing analysis is now evidenced internally by the M11 seeded playtest balance report; human playtest sign-off on its findings remains an external human gate.

This document records evidence boundaries; it does not claim release readiness or formal source-perfect acceptance.
